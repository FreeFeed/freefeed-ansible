---

freefeed_server_instances: "{{ ansible_processor_vcpus }}"
freefeed_log_dir: "/var/log/freefeed-server/{{ freefeed_env }}"
freefeed_config_dir: "/etc/freefeed"

home_dir: "/var/lib/freefeed"
shared_media_dir: "/media-tmp"
